<!DOCTYPE html>
<html lang="en">
<head>
    <title>PAY</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.3/font/bootstrap-icons.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-GLhlTQ8iRABdZLl6O3oVMWSktQOp6b7In1Zl3/Jr59b6EGGoI1aFkw7cmDA6j6gD" crossorigin="anonymous">
    <style>
        .right{
            padding: 30px;
            background-color: #ffd6ff ;
            width: 35%;
            margin-top: 100px;
            margin-left: 400px;
        }
    </style>
</head>
<body style="background-color: #c8b6ff;">
    <div class="right">
        <div>
            <h1>TICKET BOOKED <i class="bi bi-magic"></i></h1>
       </div><br>
       <div>
            {{#if from}}
            <h3>From : {{from}}</h3>
            {{/if}}
            {{#if to}}
            <h3>To : {{to}}</h3>
            {{/if}}
            {{#if ticketno}}
            <h3>Ticket no : {{ticketno}}</h3>
            {{/if}}
            {{#if busno}}
            <h3>Bus no : {{busno}}</h3>
            {{/if}}
            {{#if time}}
            <h3>Time : {{time}}</h3>
            {{/if}}
            {{#if notick}}
            <h3>No of tickets : {{notick}}</h3>
            {{/if}}
            {{#if phone}}
            <h3>Phone no : {{phone}}</h3>
            {{/if}}
       </div>
    </div>
</body>
</html>